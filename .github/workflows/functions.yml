name: functions

on:
 push:
 workflow_dispatch:
jobs:
 functions-concept:
  runs-on: ubuntu-latest
  steps:
   - name: Failing step
     run: exit 1
   - name: Success step
     if: ${{success()}}
     run: echo "I will be printed if it is success"
     
