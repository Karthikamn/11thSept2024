name: Env var

on:
 push:
 workflow_dispatch:
env:
 MY_WORKFLOW_VAR: "workflow"
jobs:
 echo_envvar:
  runs-on: ubuntu-latest
  steps:
    - name: Display Env var
      run: |
       echo "Env var: ${{env.MY_WORKFLOW_VAR}}"
